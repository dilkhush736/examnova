# Root-level env is not required by the current workspace scripts.
# Configure app-specific files instead:
# - apps/api/.env
# - apps/web/.env

# This file exists only as a pointer for developers cloning the repository.
